# Retrieving an image from JSON

- A practical task from Yandex
- A program whose task is to parse a JSON file attached to a project, extract disparate image fragments from it, and sort them using a special version of the "snake" algorithm so that they form a coherent image in horizontal orientation.

![](screenshot.png)
